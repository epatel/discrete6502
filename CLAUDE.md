# discrete6502

A hardware project to recreate the MOnSter 6502 — a working MOS 6502 CPU built from discrete surface-mount transistors — as a smaller PCB designed for JLCPCB fabrication and assembly. The logic ground truth is the visual6502 reverse-engineered netlist; the scale (~4,000+ placed parts) means schematics and placement must be generated by scripts, not drawn by hand.

Always read @project-plan.md before starting — it holds the shared goal, milestones, and current state. Update its "Current state / handoff" (and append to Decisions/Open questions) before finishing a work session.

## Cards

Load the matching card from `cards/` when a trigger fits the current task:

- `cards/architecture.md` — when designing or discussing how the board/CPU re-creation is structured, choosing between dynamic vs static logic, or deciding the netlist-to-schematic flow
- `cards/jlcpcb-constraints.md` — when making layout, package, part-selection, or cost decisions, or checking whether a design choice is manufacturable
- `cards/resources.md` — when researching the original MOnSter 6502, needing the visual6502 netlist data, or looking up parts/fab references
- `cards/visual6502-netlist.md` — when parsing or transforming the netlist files in `data/visual6502/`, needing transistor/node statistics, or checking data licensing
- `cards/monster6502-lessons.md` — when deciding how to implement a circuit block, choosing parts, or wondering what the original MOnSter did and where we deliberately diverge
- `cards/pass-pair-validation.md` — when implementing pass gates/latches, running or extending the SPICE sims in `sim/`, choosing FET/resistor parts, or estimating board size and power
- `cards/netlist-pipeline.md` — when running or modifying `tools/gen_netlist.py`, consuming `gen/netlist.json`, importing into KiCad, or checking the transform rules and part numbers
- `cards/verification.md` — when changing the netlist (must keep equivalence green), running `tools/switchsim.py` or the vendor-model SPICE sims, or reasoning about dynamic-node charge behavior
- `cards/layout.md` — when working on the PCB (`tools/gen_pcb.py`, `gen/discrete6502.kicad_pcb`), routing, DRC severity rationale, or preparing fab outputs
